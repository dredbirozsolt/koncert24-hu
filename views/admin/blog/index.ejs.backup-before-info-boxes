<!-- Admin Blog Management -->
<div class="page-container">
    <% if (typeof success !== 'undefined') { %>
        <% if (success === 'created') { %>
            <div class="alert alert-success">✓ Cikk sikeresen létrehozva!</div>
        <% } else if (success === 'updated') { %>
            <div class="alert alert-success">✓ Cikk sikeresen frissítve!</div>
        <% } else if (success === 'deleted') { %>
            <div class="alert alert-success">✓ Cikk sikeresen törölve!</div>
        <% } %>
    <% } %>

    <!-- Statistics -->
    <div class="stats-grid">
        <div class="stat-card" style="border-left-color: #10b981;">
            <h3>📝 Publikált</h3>
            <div class="stat-value"><%= posts.filter(p => p.status === 'published').length %></div>
        </div>
        <div class="stat-card" style="border-left-color: #667eea;">
            <h3>⏱️ Időzített</h3>
            <div class="stat-value"><%= posts.filter(p => p.status === 'scheduled').length %></div>
        </div>
        <div class="stat-card" style="border-left-color: #f59e0b;">
            <h3>� Piszkozat</h3>
            <div class="stat-value"><%= posts.filter(p => p.status === 'draft').length %></div>
        </div>
        <div class="stat-card" style="border-left-color: #3b82f6;">
            <h3>📊 Összes</h3>
            <div class="stat-value"><%= posts.length %></div>
        </div>
    </div>

    <!-- Filters -->
    <div class="filter-card">
        <form method="GET" action="/admin/blog">
            <div style="display: grid; grid-template-columns: 2fr 1fr auto; gap: 1rem; align-items: end;">
                <div class="form-group" style="margin-bottom: 0;">
                    <label>Keresés</label>
                    <input type="text" name="search" class="form-control"
                           placeholder="Cím vagy leírás..."
                           value="<%= search %>">
                </div>
                <div class="form-group" style="margin-bottom: 0;">
                    <label>Státusz</label>
                    <select name="status" class="form-select">
                        <option value="all" <%= status === 'all' ? 'selected' : '' %>>Összes</option>
                        <option value="published" <%= status === 'published' ? 'selected' : '' %>>Publikált</option>
                        <option value="scheduled" <%= status === 'scheduled' ? 'selected' : '' %>>⏱️ Időzített</option>
                        <option value="draft" <%= status === 'draft' ? 'selected' : '' %>>Piszkozat</option>
                        <option value="archived" <%= status === 'archived' ? 'selected' : '' %>>Archivált</option>
                    </select>
                </div>
                <div>
                    <button type="submit" class="btn btn-primary">
                        🔍 Szűrés
                    </button>
                </div>
            </div>
        </form>
    </div>

    <!-- Action Buttons -->
    <div style="margin-bottom: 1.5rem; display: flex; justify-content: flex-end; gap: 1rem;">
        <a href="/admin/blog/categories" class="btn btn-secondary">
            <i class="fas fa-folder"></i> Kategóriák
        </a>
        <a href="/admin/blog/new" class="btn btn-primary">
            ➕ Új Cikk
        </a>
    </div>

    <!-- Blog Posts Table -->
    <div class="data-card">
        <% if (posts.length === 0) { %>
            <div class="empty-state">
                <h3>😕 Nincs megjeleníthető cikk</h3>
                <p>Próbáljon más szűrési feltételeket vagy hozzon létre az első cikket.</p>
                <a href="/admin/blog/new" class="btn btn-primary" style="margin-top: 1rem;">
                    Első cikk létrehozása
                </a>
            </div>
        <% } else { %>
            <div style="overflow-x: auto;">
                <table class="data-table">
                    <thead>
                        <tr>
                            <th>Kép</th>
                            <th>Cím</th>
                            <th>Kategória</th>
                            <th>Státusz</th>
                            <th>Dátum</th>
                            <th>Műveletek</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% posts.forEach(post => { %>
                            <tr>
                                <td data-label="Kép">
                                    <% if (post.featuredImage) { %>
                                        <img src="<%= post.featuredImage %>" 
                                             alt="<%= post.title %>" 
                                             title="<%= post.title %>" 
                                             width="80" 
                                             height="50" 
                                             style="width: 60px; height: 60px; object-fit: cover; border-radius: 8px; border: 2px solid #e5e7eb;">
                                    <% } else { %>
                                        <div style="width: 60px; height: 60px; background: #f8f9fa; border-radius: 8px; display: flex; align-items: center; justify-content: center; color: #9ca3af; font-size: 2rem;">
                                            📝
                                        </div>
                                    <% } %>
                                </td>
                                <td data-label="Cím">
                                    <strong><%= post.title %></strong>
                                    <br><small style="color: #6b7280;"><%= post.slug %></small>
                                </td>
                                <td data-label="Kategória">
                                    <% if (post.category) { %>
                                        <span class="badge badge-secondary"><%= post.category.name %></span>
                                    <% } else { %>
                                        <span style="color: #9ca3af;">-</span>
                                    <% } %>
                                </td>
                                <td data-label="Státusz">
                                    <% if (post.status === 'published') { %>
                                        <span class="badge" style="background: #d1fae5; color: #065f46; font-weight: 600;">
                                            Publikált
                                        </span>
                                    <% } else if (post.status === 'scheduled') { %>
                                        <span class="badge" style="background: #ede9fe; color: #7c3aed; font-weight: 600;">
                                            Időzített
                                        </span>
                                        <% if (post.publishedAt) { %>
                                            <br><small style="color: #6b7280; font-size: 0.75rem;">
                                                <%= new Date(post.publishedAt).toLocaleString('hu-HU', { month: 'short', day: 'numeric', hour: '2-digit', minute: '2-digit' }) %>
                                            </small>
                                        <% } %>
                                    <% } else if (post.status === 'archived') { %>
                                        <span class="badge" style="background: #e5e7eb; color: #374151; font-weight: 600;">
                                            Archivált
                                        </span>
                                    <% } else { %>
                                        <span class="badge" style="background: #fef3c7; color: #92400e; font-weight: 600;">
                                            Piszkozat
                                        </span>
                                    <% } %>
                                </td>
                                <td data-label="Dátum" style="white-space: nowrap;"><%= new Date(post.createdAt).toLocaleDateString('hu-HU') %></td>
                                <td data-label="Műveletek">
                                    <div class="actions">
                                        <a href="/blog/<%= post.slug %>" target="_blank" class="btn-icon" style="color: #3b82f6;" title="Megtekintés">
                                            👁
                                        </a>
                                        <a href="/admin/blog/<%= post.id %>/edit" class="btn-icon" style="color: #3b82f6;" title="Szerkesztés">
                                            ✏️
                                        </a>
                                        <button class="btn-icon delete-post" 
                                                data-id="<%= post.id %>" 
                                                data-title="<%= post.title %>"
                                                style="color: #ef4444;"
                                                title="Törlés">
                                            🗑️
                                        </button>
                                    </div>
                                </td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>

            <% if (totalPages > 1) { %>
                <div style="padding: 1.5rem; display: flex; justify-content: center; gap: 0.5rem; align-items: center;">
                    <% if (hasPrevPage) { %>
                        <a href="?page=<%= currentPage - 1 %>&status=<%= status %>&search=<%= search %>" class="btn btn-secondary">← Előző</a>
                    <% } %>
                    <span style="padding: 0 1rem; color: var(--text-muted); font-weight: 500;">
                        <%= currentPage %> / <%= totalPages %>
                    </span>
                    <% if (hasNextPage) { %>
                        <a href="?page=<%= currentPage + 1 %>&status=<%= status %>&search=<%= search %>" class="btn btn-secondary">Következő →</a>
                    <% } %>
                </div>
            <% } %>
        <% } %>
    </div>
</div>

<script>
document.querySelectorAll('.delete-post').forEach(btn => {
    btn.addEventListener('click', async function() {
        const title = this.dataset.title;
        const id = this.dataset.id;
        
        const confirmed = await UniversalModal.confirmDialog(
            `Biztosan törölni szeretnéd ezt a cikket?\n\n"${title}"`,
            { confirmText: 'Törlés', cancelText: 'Mégse' }
        );
        
        if (confirmed) {
            const form = document.createElement('form');
            form.method = 'POST';
            form.action = `/admin/blog/${id}/delete`;
            
            const csrfInput = document.createElement('input');
            csrfInput.type = 'hidden';
            csrfInput.name = '_csrf';
            csrfInput.value = '<%= csrfToken %>';
            
            form.appendChild(csrfInput);
            document.body.appendChild(form);
            form.submit();
        }
    });
});
</script>
